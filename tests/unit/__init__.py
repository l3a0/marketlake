"""Unit tier.

One module, every seam faked, decided from values alone. These are the fast tests
that run on every save. The two enforcement guards live here too. They are decided
from the source itself, so they belong with the value-decided tests.
"""
