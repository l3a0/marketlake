"""Integration tier.

Two or more subsystems wired through real boundaries. These run on every push. The
tier is empty in D0. The 14 named integration tests begin with the slice-1 cycle end
to end at deliverable D7, per docs/build-plan.md. The folder exists now so those tests
have a home the moment the first one lands.
"""
